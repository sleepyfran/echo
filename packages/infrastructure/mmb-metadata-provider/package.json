{
  "name": "@echo/infrastructure-mmb-metadata-provider",
  "private": true,
  "version": "1.0.0",
  "description": "Implements the metadata provider interface using the music-metadata (previously music-metadata-browser, hence the mmb naming) library",
  "main": "index.js",
  "scripts": {
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@echo/core-types": "^1.0.0",
    "buffer": "^6.0.3",
    "effect": "^3.5.8",
    "music-metadata": "^10.0.0",
    "process": "^0.11.10"
  }
}