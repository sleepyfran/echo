{
  "name": "@echo/core-config",
  "private": true,
  "version": "1.0.0",
  "description": "Contains static configuration values for the app.",
  "main": "index.js",
  "scripts": {
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@echo/core-types": "^1.0.0"
  }
}