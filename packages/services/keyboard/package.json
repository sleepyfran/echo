{
  "name": "@echo/services-keyboard",
  "private": true,
  "version": "1.0.0",
  "description": "Contains a pubsub that can be listened from the UI to react to global keyboard events.",
  "main": "index.js",
  "scripts": {
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@echo/core-types": "workspace:*",
    "@echo/core-config": "workspace:*",
    "effect": "4.0.0-rc.112"
  }
}
